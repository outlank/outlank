{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile current file using babel",
      "type": "shell",
      "command": "babel ${file} --out-file ${workspaceFolder}\\.compiled\\temp\\single-file-debug.js --source-maps",
      "problemMatcher": []
    }
  ]
}
